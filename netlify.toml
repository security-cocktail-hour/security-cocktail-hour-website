# Netlify configuration for Security Cocktail Hour website
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  # Build command
  command = "hugo --minify"

  # Output directory (Hugo generates static files here)
  publish = "public"

[build.environment]
  # Hugo version (should match your local version: v0.151.0)
  HUGO_VERSION = "0.151.0"
  HUGO_ENV = "production"

# Block search engines on all Netlify deployments by default
# This prevents staging sites from being indexed
# Production (GoDaddy cPanel) does not use these headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"

# Enforce trailing slashes for SEO consistency
# Redirect non-trailing-slash URLs to trailing-slash versions (301 permanent)
# This prevents duplicate content issues
[[redirects]]
  from = "/:splat"
  to = "/:splat/"
  status = 301
  force = false
  conditions = {path = ["!*.*", "!/"]}

# 404 redirect (must come after trailing slash redirect)
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache static assets
[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
